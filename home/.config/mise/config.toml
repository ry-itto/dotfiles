[settings]
idiomatic_version_file_enable_tools = ["ruby"]
# Required by `mise bootstrap` (experimental). On a fresh machine this file does
# not exist yet, so the first bootstrap must be run with MISE_EXPERIMENTAL=1.
experimental = true
